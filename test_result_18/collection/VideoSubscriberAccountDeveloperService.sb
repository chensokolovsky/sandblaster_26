(version 1)
(allow default)
(deny file-read*
	(require-all
		(require-all (require-not (subpath-prefix "${HOME}/Library/com.apple.VideoSubscriberAccount.DeveloperService"))
(require-not (subpath-prefix "${HOME}/Documents/com.apple.VideoSubscriberAccount.DeveloperService")))
		(require-not (subpath "/private/var/tmp"))
		(subpath-prefix "${PROCESS_TEMP_DIR}")))
(deny file-write*
	(require-all
		(require-all (require-not (subpath-prefix "${HOME}/Library/com.apple.VideoSubscriberAccount.DeveloperService"))
(require-not (subpath-prefix "${HOME}/Documents/com.apple.VideoSubscriberAccount.DeveloperService")))
		(require-not (subpath "/private/var/tmp"))
		(subpath-prefix "${PROCESS_TEMP_DIR}")))
(deny file-write-setugid)
(deny file-write-unlink
	(entitlement-is-present com.apple.private.oop-jit.loader)
	(require-all
		(extension com.apple.sandbox.oopjit)
		(require-not (subpath "/private/var/OOPJit"))))
(deny job-creation)
(deny system-kas-info)
(deny storage-class-map)
